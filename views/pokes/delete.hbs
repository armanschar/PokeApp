<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-body text-center">
                <h3 class="card-title text-danger">
                    <i class="bi bi-exclamation-triangle"></i> Confirmar Eliminación
                </h3>
                
                <div class="mb-3">
                    <img src="{{poke.pokeimg}}" alt="{{poke.name}}" 
                         class="img-thumbnail" style="width: 150px; height: 100px; object-fit: cover;">
                </div>
                
                <p class="card-text">
                    ¿Está seguro que desea eliminar este Pokemon?
                </p>
                
                <div class="alert alert-info">
                    <strong>{{poke.name}}</strong><br>
                    <small class="text-muted">
                        Tipo: {{getTypeName typesList poke.typeId}}
                    </small></br>
                    <small class="text-muted">
                        Región: {{getRegionName regionsList poke.regionId}}
                    </small>
                </div>

                
                <form method="POST" action="/pokes/delete" class="d-inline">
                    <input type="hidden" name="PokesId" value="{{poke.id}}">
                    <a href="/pokes" class="btn btn-secondary me-2">
                        <i class="bi bi-x-circle"></i> Cancelar
                    </a>
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Eliminar
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>